spring:
  cloud:
    config:
      label: main
      profile: dev
      discovery:
        enabled: true
        serviceId: config-server
#    bus:
#      enabled: true
#      trace:
#        enabled: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
#添加kafka配置
#kafka:
#  bootstrap-servers: localhost:9092
#  topic: springcloud

